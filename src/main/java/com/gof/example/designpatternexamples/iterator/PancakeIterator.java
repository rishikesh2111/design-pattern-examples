package com.gof.example.designpatternexamples.iterator;

import java.util.List;

/*public class PancakeIterator implements Iterator {
        List<MenuItem> menuItems;
        java.util.Iterator iteratorList = null;

        public PancakeIterator(List<MenuItem> menuItems) {
            this.menuItems = menuItems;
            this.iteratorList = menuItems.iterator();
        }

        @Override
        public boolean hasNext() {
            return iteratorList.hasNext();
        }

        @Override
        public String next() {
            MenuItem menuItem = (MenuItem)iteratorList.next();
            System.out.println(menuItem);
            return menuItem.toString();
        }

        @Override
        public void remove() {

        }
    }*/
